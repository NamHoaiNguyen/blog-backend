const kong_authorize_endpoint_suffix = "/oauth2/authorize"

const kong_token_endpoint_suffix = "/oauth2/token"

const kong_route_login_path = "/login-route"

export { 
  kong_authorize_endpoint_suffix, 
  kong_token_endpoint_suffix, 
  kong_route_login_path }